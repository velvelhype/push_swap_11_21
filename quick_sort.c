#include "push_swap.h"

void    quick_sort(t_list *a_head, t_list *b_head)
{
    // init stack : send half of stack
    init_stack(a_head, b_head);
    // send smallest number to the last of the list
}
